<!-- CLI API (Javadoc) -->
<iframe
  id="cli-api-frame"
  data-target="../../../javadoc/cli/index.html"
  style="width:100%;height:85vh;border:0;"
  loading="lazy"></iframe>

<!-- Keep a Markdown link that MkDocs can validate (no warnings) -->
[Open full screen →](../../javadoc/cli/index.html)

<script>
  (function () {
    var f = document.getElementById('cli-api-frame');
    if (f && f.dataset && f.dataset.target) {
      // Use the runtime-only target that we know works in both mkdocs serve and GH Pages
      f.src = f.dataset.target;
    }
  })();
</script>
